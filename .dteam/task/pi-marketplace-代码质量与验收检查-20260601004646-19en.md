# pi-marketplace 代码质量与验收检查

## 基本信息
- ID: 20260601004646-19en
- 类型: bugfix
- 创建时间: 2026-05-31T16:46:46.808Z
- 状态: todo

## 目标
- 为什么: 当前 task 系统为空，但需要沉淀本次代码质量检查、验收结论和后续修复建议。
- 做什么: 对 pi-marketplace 代码与文档进行质量审查，验证实现结果，并将 PASS/FAIL/BLOCKER 结论写入 task 记录。

## 范围
- 包含: 
- 排除: 

## 验收条件（GWT + 测试）
说明：原 task 系统为空，原始「验收条件」缺失；以下为本次修复后的 fallback 验收项，并按 GWT + PASS/FAIL/BLOCKER 记录。


- [PASS] AC-01 严格类型检查通过
  - Given 当前仓库源码
  - When 执行 `npx tsc --noEmit --strict --moduleResolution bundler --module esnext --target es2022 --skipLibCheck extensions/*.ts extensions/tools/*.ts`
  - Then 编译应无错误
  - 结果：PASS
  - 证据：命令无输出并成功退出

- [PASS] AC-02 搜索结果返回正确下载量
  - Given npm search API 返回的下载量在 `objects[].downloads`
  - When `extensions/api.ts` 的 `searchPackages()` 解析搜索结果
  - Then `downloads.monthly/weekly` 应与 registry 响应一致
  - 结果：PASS
  - 证据：实测 `searchPackages('mcp',{limit:1})` 返回 `{"monthly":98254,"weekly":25627}`

- [PASS] AC-03 包详情读取正确的 latest 版本与元数据
  - Given npm package detail 的 canonical latest 版本在 `dist-tags.latest`，发布时间在 `time[version]`
  - When `getPackageDetail()` / `getPkgPiManifest()` 解析 registry 详情
  - Then 应返回正确的 latest 版本、publisher、date、links、manifest
  - 结果：PASS
  - 证据：实测 `pi-mcp-adapter` 输出 `version=2.8.0`、`publisher=nicopreme`、`published=6d ago`，且 homepage/repository 链接正常

- [PASS] AC-04 深度审计覆盖发布包中的实际运行时代码
  - Given npm 包常把运行时代码发布在 `dist/`
  - When `sourceScan()` 深度扫描 tarball
  - Then 不应跳过 `dist/` 目录中的 `.js/.mjs/.cjs`
  - 结果：PASS
  - 证据：构造本地 fixture `/tmp/pi-audit-fixture/dist/index.js` 含 `eval(input)`，`sourceScan()` 成功报出 `dist/index.js:2`

- [PASS] AC-05 安装超时或被杀死时不会回报安装成功
  - Given `pi install` 可能超时或被 OS 杀死
  - When `marketplace_install` 处理 `pi install` 子进程错误
  - Then 工具应返回失败，而不是 success message
  - 结果：PASS
  - 证据：`extensions/tools/install.ts` 已改为 `execFile()`，且任意 `err` 都会 reject；不会再走 success 分支
  - 备注：该项已做代码路径验证，未单独构造 timeout e2e 场景

- [PASS] AC-06 文档与代码实现一致
  - Given README 不应承诺未接入的能力
  - When 检查 `README.md`、`package.json`、`extensions/index.ts`
  - Then 文档应描述当前真实能力：registry-first + gallery links + security audit
  - 结果：PASS
  - 证据：已移除 optional enrichment / tool-agnostic enrichment 相关表述，并更新开发命令为 `pi --no-extensions -e .`

- [PASS] AC-07 类型过滤在大结果集下避免无界并发请求
  - Given `limit` 最大可到 250
  - When `marketplace_search` 在 type filter 模式下拉取每个包的 manifest
  - Then 应限制并发，避免一次性打满 registry 请求
  - 结果：PASS
  - 证据：`extensions/tools/search.ts` 已改为 `mapWithConcurrency(..., 8, ...)`

- [PASS] AC-08 完整端到端工具调用验证
  - Given 当前机器已全局安装同名 `pi-marketplace`
  - When 使用隔离方式 `./node_modules/.bin/pi --no-extensions -e . -p ...` 执行真实 tool call
  - Then 应能完成搜索 / 详情 / 审计调用
  - 结果：PASS
  - 证据：
    - 搜索：返回 `pi-mcp-adapter`
    - 详情：返回 `nicopreme 6d ago`
    - 审计：`marketplace_audit deepScan=false` 返回 `INFO`

## 阶段记录### 探索发现
- 初始 `task_list` 结果为空，无法读取既有 task；已创建 fallback task：`20260601004646-19en`
- 仓库是一个 pi package / extension，核心代码位于 `extensions/` 与 `extensions/tools/`
- 当前仓库原本无自动化测试；本轮补充了最小集成测试，覆盖 registry 解析与安全扫描核心路径

### 讨论决策
- 由于没有现成 task 与验收项，采用“创建 fallback task + 在验收条件中沉淀检查项”的方式保留本次审查记录
- 修复策略优先级：先修正确性（registry 解析）、再修安全覆盖（dist 扫描 / shell 调用）、再收口文档与并发控制
- 自动化测试采用 Node 内置 `node:test`，避免额外引入测试依赖；测试前临时编译 TypeScript 到 `.tmp/test-build`
- 端到端验证改为 `pi --no-extensions -e .`，避免与本机已安装的同名 package 冲突

### 执行记录
- 初始检查：
  - 读取并检查文件：`package.json`、`README.md`、`extensions/api.ts`、`extensions/enrich.ts`、`extensions/format.ts`、`extensions/security.ts`、`extensions/tools/*.ts`
  - 运行类型检查：`npx tsc --noEmit --strict --moduleResolution bundler --module esnext --target es2022 --skipLibCheck extensions/*.ts extensions/tools/*.ts`
  - 运行打包检查：`npm pack --dry-run`
- 修复内容：
  1. `extensions/api.ts`
     - 修正 npm search 下载量字段来源：`obj.downloads`
     - 改为使用 `dist-tags.latest` 解析 latest version
     - 补齐 `publisher/date/links` 的正确映射与 fallback
  2. `extensions/security.ts`
     - 深度扫描不再跳过 `dist/`
     - `exec()` 改为 `execFile()`，降低 shell 注入与 quoting 风险
     - 扫描结果路径改为相对路径，输出更稳定
  3. `extensions/tools/install.ts`
     - `exec()` 改为 `execFile()`
     - 任意安装错误（含 killed/timeout）都返回失败，不再误报成功
  4. `extensions/tools/search.ts`
     - 为 type filter 增加并发受控的 manifest 拉取（并发 8）
  5. `extensions/format.ts`
     - `formatDate()` 增加空值 / invalid date 兜底
  6. 自动化测试
     - 新增 `tests/marketplace.test.mjs`
     - 新增 `npm test` / `npm run typecheck`
     - 覆盖：搜索下载量解析、detail latest 元数据解析、`dist/` 扫描
  7. 文档
     - 更新 `README.md`、`package.json`、`extensions/index.ts`，删除未实现的 enrichment 承诺，改为当前真实能力描述
- 修复后复验：
  - `npm test` → PASS（3/3）
  - `npm run typecheck` → PASS
  - `npm pack --dry-run` → PASS
  - 临时编译到 `/tmp/pi-marketplace-check` 后运行脚本：
    - `searchPackages('mcp',{limit:1})` → `{"monthly":98254,"weekly":25627}`
    - `getPackageDetail('pi-mcp-adapter')` → `publisher=nicopreme`、`date=2026-05-25T06:32:22.157Z`、links 正常
  - 构造 `/tmp/pi-audit-fixture/dist/index.js` 含 `eval(input)`，执行 `sourceScan('/tmp/pi-audit-fixture')` → 成功命中 `dist/index.js:2`
  - 端到端：
    - `./node_modules/.bin/pi --no-extensions -e . -p "Use marketplace_search ..."` → 返回 `pi-mcp-adapter`
    - `./node_modules/.bin/pi --no-extensions -e . -p "Use marketplace_detail ..."` → 返回 `nicopreme 6d ago`
    - `./node_modules/.bin/pi --no-extensions -e . -p "Use marketplace_audit ... deepScan false"` → 返回 `INFO`

### 收口记录
- 当前结论：**本轮修复完成，已通过本地复验，并补齐最小自动化测试**
- 剩余说明：
  - `extensions/enrich.ts` 仍为未接入的辅助代码；当前文档已不再承诺该能力
  - `package-lock.json` 为未跟踪文件，未纳入本次修复范围
- 建议下一步：
  1. 若确认不再做 pi.dev enrichment，可删除 `extensions/enrich.ts`
  2. 如需更强验证，可继续为 install 路径补 mock / timeout 测试

